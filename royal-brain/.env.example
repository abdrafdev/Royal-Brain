# Royal BrAIn™ Environment Variables (Template)
# Copy to .env.dev / .env.staging / .env.production at the repo root.

# A sovereign system never mixes environments.
RB_ENV=dev

# ===== Backend =====
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
DATABASE_URL=sqlite:///./rb_dev.db

# JWT auth
JWT_SECRET=CHANGE_THIS_TO_RANDOM_32+_CHAR_STRING_USE_openssl_rand_hex_32
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# AI (optional)
OPENAI_API_KEY=

# EVM blockchain anchoring (optional)
# Configure to enable real on-chain anchoring via app.trust.blockchain
EVM_RPC_URL=
EVM_CHAIN_ID=
EVM_PRIVATE_KEY=
EVM_EXPLORER_TX_URL_BASE=

# CORS (comma-separated)
CORS_ORIGINS=http://localhost:3000

# First-run authority bootstrap (optional)
BOOTSTRAP_ADMIN_EMAIL=
BOOTSTRAP_ADMIN_PASSWORD=

# ===== Frontend (Next.js) =====
# Public backend URL for browser calls
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
# Legacy/alternate variable used by some server-side helpers
BACKEND_URL=http://localhost:8000

NEXT_PUBLIC_APP_NAME=Royal BrAIn
