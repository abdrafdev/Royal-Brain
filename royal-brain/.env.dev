# Royal BrAIn — Development Environment
# A sovereign system never mixes environments.
RB_ENV=dev

# Backend
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
DATABASE_URL=sqlite:///./rb_dev.db

JWT_SECRET=dev-change-me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# AI (optional)
# Set this in your local environment / secret manager (do not commit real keys).
OPENAI_API_KEY=

# Comma-separated
CORS_ORIGINS=http://localhost:3000

# First-run authority bootstrap (optional)
BOOTSTRAP_ADMIN_EMAIL=admin@royalbrain.dev
BOOTSTRAP_ADMIN_PASSWORD=change-me-now

# Frontend (Next.js)
BACKEND_URL=http://localhost:8000
NEXT_PUBLIC_APP_NAME=Royal BrAIn
