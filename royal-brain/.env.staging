# Royal BrAIn — Staging Environment
# A sovereign system never mixes environments.
RB_ENV=staging

# Backend
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST:5432/royal_brain

JWT_SECRET=staging-change-me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Comma-separated
CORS_ORIGINS=https://staging.example.com

# First-run authority bootstrap (optional)
BOOTSTRAP_ADMIN_EMAIL=
BOOTSTRAP_ADMIN_PASSWORD=

# Frontend (Next.js)
BACKEND_URL=https://staging-api.example.com
NEXT_PUBLIC_APP_NAME=Royal BrAIn (Staging)
